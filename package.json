{
  "name": "daily-ledger",
  "version": "1.0.0",
  "description": "The Daily Ledger — card generation pipeline",
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-cards.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@supabase/supabase-js": "^2.49.0"
  }
}
